{
  "name": "claygardenpress",
  "version": "0.1.0",
  "description": "Clay Garden Press — field notes and deep dives",
  "private": true,
  "scripts": {
    "build": "eleventy",
    "dev": "eleventy --serve --port 8080"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "@11ty/eleventy-plugin-rss": "^2.0.2"
  }
}
